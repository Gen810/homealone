services:
  python:
    build:
      context: .
      dockerfile: Dockerfile
    image: python:3.10
    container_name: python3.10
    hostname: homealone
    volumes:
      - ../:/workspace
    tty: true